npm install -g firebase-tools
firebase login
firebase init hosting
{
  "hosting": {
    "public": "dist",  // Change to your build output folder (e.g., "build", "public")
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
